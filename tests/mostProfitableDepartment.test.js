describe('The mostProfitableDepartment', function(){
  it("If the test passes it should return the department", function(){
      assert.notEqual('mostProfitableDepartment', true);
  });
  it('If the test fails it should return the sales', function(){
    assert.notDeepEqual('mostProfitableDepartment', true);
  })

});
